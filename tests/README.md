# Tests

This directory contains the automated testing suite for SecondSpin.

Currently, it tests the backend API endpoints using `pytest`. Tests can be run independently of the development server.

## Running Tests

From the repository root, run:
```bash
pytest tests/
```

This will run all configured test cases inside this folder.
